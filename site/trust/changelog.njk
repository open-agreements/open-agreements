---
layout: base.njk
title: Changelog | OpenAgreements
description: Public release history with auto-generated notes from GitHub releases.
---

<main class="main-wrap">
  <section class="section" style="padding: clamp(32px, 5vw, 56px) clamp(16px, 4vw, 48px);">
    <h1>Changelog</h1>
    <p style="color: var(--ink-soft); max-width: 700px; margin: 8px 0 8px;">
      This changelog is generated during site builds from GitHub Releases.
      It is the public release history for OpenAgreements.
    </p>
    <p style="color: var(--ink-soft); font-size: 0.86rem; margin: 0 0 28px;">
      Last refreshed: <code>{{ changelog.generated_at_utc if changelog and changelog.generated_at_utc else "unknown" }}</code>
      {% if changelog and changelog.source %}
      &middot; source: <code>{{ changelog.source }}</code>
      {% endif %}
    </p>

    {% set releases = [] %}
    {% if changelog and changelog.releases %}
      {% set releases = changelog.releases %}
    {% endif %}

    {% if releases.length %}
      {% for release in releases %}
        <article style="border: 1px solid var(--line); background: var(--bg); padding: 16px; margin-bottom: 14px;">
          <h2 style="margin: 0 0 6px; font-size: 1.02rem;">
            {% if release.url %}
              <a href="{{ release.url }}" target="_blank" rel="noreferrer" style="color: var(--accent-deep); text-decoration: none;">
                {{ release.title or release.tag or ("Release " + loop.index) }}
              </a>
            {% else %}
              {{ release.title or release.tag or ("Release " + loop.index) }}
            {% endif %}
          </h2>
          <p style="color: var(--ink-soft); font-size: 0.84rem; margin: 0 0 10px;">
            {% if release.tag %}<code>{{ release.tag }}</code>{% endif %}
            {% if release.published_on %}
              {% if release.tag %}&middot;{% endif %}
              {{ release.published_on }}
            {% endif %}
            {% if release.prerelease %}
              &middot; prerelease
            {% endif %}
          </p>

          {% if release.notes_available %}
            <details{% if loop.index0 == 0 %} open{% endif %}>
              <summary style="cursor: pointer; color: var(--ink); font-weight: 600;">Release notes</summary>
              <div class="release-notes" style="margin: 12px 0 0; font-size: 0.9rem; line-height: 1.55;">
                {{ release.notes | renderMarkdown | safe }}
              </div>
            </details>
          {% else %}
            <p style="margin: 0; color: var(--ink-soft); font-size: 0.9rem;">No notes available for this release.</p>
          {% endif %}
        </article>
      {% endfor %}
    {% else %}
      <p style="color: var(--ink-soft); margin-top: 14px;">
        No release entries are available yet.
      </p>
    {% endif %}

    <p style="margin-top: 20px;">
      <a href="/trust/" style="color: var(--accent-deep);">&larr; Back to Trust</a>
    </p>
  </section>
</main>
