# signing Specification

## Purpose
Reserved for the signing capability. Detailed requirements are tracked in the active `add-agreement-signing` change until that work is rebased onto this capability.

## Requirements
### Requirement: Signing Capability Stub
The signing capability SHALL remain reserved for generic signing-provider
requirements while detailed signing behavior is tracked in the active
`add-agreement-signing` change.

#### Scenario: [OA-SIG-000] Signing requirements remain in active change
- **WHEN** maintainers review signing behavior before `add-agreement-signing` is archived
- **THEN** detailed signing requirements are read from the active change package
