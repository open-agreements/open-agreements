# open-agreements Specification

## Purpose
Tombstone: legacy monolith emptied by `restructure-specs-modular`; see modular capabilities under `openspec/specs/`.

## Requirements
### Requirement: Legacy Monolith Tombstone
The `open-agreements` capability SHALL remain as a tombstone that points
readers to the modular capability specs.

#### Scenario: [OA-LEG-001] Reader follows modular capability specs
- **WHEN** a reader opens the legacy `open-agreements` spec
- **THEN** the spec points to the modular capabilities
