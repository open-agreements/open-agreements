# Shared skill assets

This directory holds content shared by several agreement skills (e.g. the
template-filling workflow referenced from their SKILL.md files). It is **not**
an installable skill — there is intentionally no SKILL.md here, so skill
installers and registry publishes skip it.
